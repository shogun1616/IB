import os
import subprocess
import sys

# instalador automático (solo se ejecuta si falta matplotlib)
required = ["matplotlib", "numpy", "scipy", "pandas"]

for pkg in required:
    try:
        __import__(pkg)
    except ImportError:
        subprocess.check_call([sys.executable, "-m", "pip", "install", pkg])

import matplotlib.pyplot as plt
import numpy as np
from scipy.optimize import curve_fit
import pandas as pd
